machine:
  node:
    version: "6.10"

dependencies:
  pre:
    - cd packages/lambda && npm install
    - cd packages/serverless-plugin && npm install
    - cd examples/serverless-framework/aws && npm install

test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/reports
    - cd packages/lambda && npm run test:integration
